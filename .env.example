# Optional: if set, server uses Postgres. If omitted, local SQLite is used.
DATABASE_URL=postgres://user:pass@host:5432/dbname

# Optional local port
PORT=3000

# Frontend: enable sync against backend API (/api/state).
# Default is local-only storage in browser.
VITE_REMOTE_SYNC=1
